different classes with priority
	shell (when one want to use the program as shell)
	print (when information wants to be printed)
	test (for testing all the classes)
	step (creates a step in a backlog)
		subclasses ex. school, food, fun ...
	create (for creating something new, like sprint or step)
	delete (..)
	save (saving information to file)
	read (reading from file)

format for the saving of data:
	first number saying class
	next lines represent attributes for the class, if array first number represents how many elements
	repeat till EOF
